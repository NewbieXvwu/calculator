// 15. Unicode 输入
2+3;
sin(π/2);
cos(π);
√(2);
√2;
x²+2*x+1;
x^2+2*x+1;
e^x;
exp(x);
∞;
limit(1/x,x,0);
lim(1/x,x,0);
α+β;
sin(θ);
diff(sin(θ),θ);
θ^2+1;
γ;
Δ;
√(x^2+1);
∫x^2;
∂x(x^3);
e;
π;
2π;
e^(i*π);
e^(i*π)+1;
ℝ;
ℕ;
ℤ;
𝕀;
x∈ℝ;
x∈ℚ;
∅;
∈;
α;
β;
γ;
θ;
λ;
μ;
σ;
ω;
≤;
≥;
≠;
±;
×;
÷;
∘;
−;
∑;
∏;
log10(100);
log2(8);
ln(e^2);
mod(17,5);
5 mod 3;
sin(π/4)+cos(π/4);
