// 4. 方程求解
solve(x^2-1);
solve(x^2-5*x+6);
solve(x^2+1);
solve(x^2+x+1);
solve(x^3-1);
solve(x^3-3*x+1);
solve(x^4-1);
solve(x^4-5*x^2+4);
solve(x^4+x^3+x^2+x+1);
solve(x^3-2);
solve(x^5-1);
solve(x^6-1);
solve(x^3-6*x^2+11*x-6);
solve((x-1)^2);
solve(2*x+3);
solve(0*x+3);
solve(a*x+b);
solve(x^2=p);
solve(sin(x));
solve(cos(x));
solve(tan(x));
solve(sin(x)=1/2);
solve(sin(x)=1/2,x);
solve(cos(x)=1/2);
solve(exp(x)=2);
solve(exp(x)=2,x);
solve(ln(x)=1);
solve(ln(x)=1,x);
solve(sin(2*x)=sin(x));
solve(sin(x)+cos(x)=1);
solve(x^2=2^x);
solve(2^x=3);
solve(2^x=3,x);
solve(sqrt(x)=2);
solve(sqrt(x^2+1)=2);
solve(sqrt(x)+sqrt(x+1)=3);
solve(abs(x)=2);
solve(abs(x-1)<3);
solve(abs(x-1)<3,x);
solve(x^2<4);
solve(x^2<4,x);
solve(1/x>0);
solve(1/x>0,x);
solve(x^2-1>=0,x);
solve(2*x<6);
solve(sin(x)>1/2,x);
solve(1/x<1,x);
solve((x-1)*(x+2)>0,x);
solve([x+y=3,x-y=1],[x,y]);
solve([x+y+z=6,2*x+y-z=1,x-y+2*z=3],[x,y,z]);
solve([x^2+y^2=25,x-y=1],[x,y]);
solve([x+y=1,x^2+y^2=1],[x,y]);
solve([x*y=6,x+y=5],[x,y]);
solve([sin(x)=1/2,cos(x)=1/2],[x]);
solve([x+y=a,x-y=b],[x,y]);
solve(x^2-3*x+2>0,x);
solve(x^2+x+1>0,x);
solve(x^3-4*x<0,x);
solve((x^2-4)/(x-1)>0,x);
solve(sin(x)=0,x);
solve(cos(x)=0,x);
solve(tan(x)=1,x);
solve(sin(x)=1,x);
solve(cos(x)=-1,x);
solve(x=0,x);
solve(2^x=2^(x+1));
solve(2^x=2^(x+1),x);
solve(4^x=2^(x+1),x);
solve(e^(2*x)-3*e^x+2=0,x);
solve(ln(x)+ln(x-1)=0,x);
solve(log(x,2)=3,x);
solve(3^(x+1)=27,x);
csolve(x^2+1);
csolve(x^2+x+1);
csolve(x^3+1);
csolve(x^2+2*x+5=0);
csolve(exp(x)=-1);
csolve(x^4+1);
csolve(x^2-2*i*x-1);
linsolve([x+y=3,x-y=1],[x,y]);
linsolve([2*x+3*y=7,x-y=1],[x,y]);
linsolve([x+y+z=6,2*x+y-z=1,x-y+2*z=3],[x,y,z]);
linsolve([x+2*y+3*z=14,2*x+3*y+z=11,3*x+y+2*z=11],[x,y,z]);
linsolve([x+y=1,x+y=2],[x,y]);
linsolve([x+y+z=0,2*x+2*y+2*z=0,3*x+3*y+3*z=0],[x,y,z]);
fsolve(cos(x)=x,x);
fsolve(sin(x)=x/2,x);
fsolve(x^3-2*x-5,x);
fsolve(exp(x)=10-x,x);
fsolve(x^5-x-1,x);
fsolve(cos(x)=x,x,0.5);
fsolve(cos(x)=x,x,-1,1);
fsolve(x^3-2*x-5,x,-10,10);
desolve(y'=y,y,x);
desolve(y'=y,y(0)=1);
desolve(y'=-2*y,y,x);
desolve(y''+y=0,y,x);
desolve(y''-y=0,y,x);
desolve(y''+2*y'+y=0,y,x);
desolve(y''+y=sin(x),y,x);
desolve(y'=y^2,y,x);
desolve(y'=x+y,y,x);
desolve(y'=y+x^2,y,x);
desolve(y'=sin(x)*y,y,x);
desolve(y''=x,y,x);
desolve(y''-3*y'+2*y=0,y,x);
desolve(y'=2*x,y(1)=3);
desolve(y''+y=0,y(0)=1,y'(0)=0);
